output: ../build/dist/
releases:
  - name: linux
    jobs:
      - name: deb
        package:
          platform: linux
          target: deb
      - name: rpm
        package:
          platform: linux
          target: rpm
      - name: zip
        package:
          platform: linux
          target: zip
  # Experimental:
  # - name: windows
  #   jobs:
  #     - name: msix
  #       package:
  #         platform: windows
  #         target: msix
  #     - name: exe
  #       package:
  #         platform: windows
  #         target: exe
  # - name: macos
  #   jobs:
  #     - name: dmg
  #       package:
  #         platform: macos
  #         target: dmg
